name: Build Plugin

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout repository
      - name: Checkout
        uses: actions/checkout@v4

      # Set up Java 17 (required for Paper 1.19.2)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # Verify Gradle is available
      - name: Verify Gradle
        run: gradle -v

      # Build the plugin JAR
      - name: Build with Gradle
        run: gradle build

      # Upload the jar as an artifact
      - name: Upload Plugin Jar
        uses: actions/upload-artifact@v4
        with:
          name: block-randomizer-reloaded-jar
          path: build/libs/*.jar
